# Execution plan

1. F0: shared score/tooltip/badge and responsive result foundations.
2. F1: result context, CV and cutoff/fallback signals.
3. F2: one pass-score field and read-only scoring-law explanation.
4. F3: system-default rubric preview, levels, `id` echo and `minPct`.
5. F4: question groups, “Luôn hỏi”, K-per-candidate and adaptive presets.
6. F5: editable job-needs panel and `isMustHave` contract.
7. F6: detail card, product contract and story references.

Each increment is validated and committed independently before the next increment.
