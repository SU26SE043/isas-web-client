/** Estimated tokens held when a B2C practice session is created (BR-B2C-03). */
export const PRACTICE_RESERVE_ESTIMATE = 800;

/** Mock actual tokens charged after a scored report (BR-B2C-04). */
export const MOCK_SETTLE_ACTUAL_TOKENS = 620;
